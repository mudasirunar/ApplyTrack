[build]
  base = "WebApp"
  command = "npm run build"
  publish = "dist"

# Only rebuild when WebApp files change (skip Android-only pushes)
[build.processing]
  skip_processing = false

[context.production]
  ignore = "git diff --quiet $CACHED_COMMIT_REF $COMMIT_REF -- ."
